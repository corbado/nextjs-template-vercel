import LoginForm from "./login-form";

export default function LoginPage() {
  return (
    <div className="relative z-0 flex flex min-h-screen items-center justify-center">
      <LoginForm />
    </div>
  );
}
